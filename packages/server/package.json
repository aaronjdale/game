{
    "name": "server",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "start": "bun run index.ts",
        "build": "bun build --entrypoints ./index.ts --outdir ./dist --target node --sourcemap=external --minify-whitespace --minify-identifiers --minify-syntax"
    },
    "dependencies": {
        "math": "workspace:*"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
